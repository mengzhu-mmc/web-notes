> git log 查看提交的各个版本的信息

> git reset --hard 版本号  回滚到指定的版本

> git reflog 查看所有提交的版本信息(包括回滚记录)

> git reset --hard 版本号 回滚到原先的版本

> git checkout -- 文件名   文件修改但未提交到暂存区时，用此命令放弃之前的修改

> git rest HEAD 文件名  文件修改并提交到暂存区后，用此命令使文件恢复到未提交的状态


