# 2026-03-24 日记

## 钉钉机器人故障排查（23:00 前后）

### 故障现象
- 21:50 后钉钉机器人完全不回复私聊消息
- 群聊 @ 机器人正常回复

### 根本原因（两个叠加问题）

1. **WebSocket 连接断开**：21:50 前后 gateway WebSocket 意外断开，未自动重连
2. **私聊 session deliveryContext 损坏**：session `a4ee8b9a-cf54-4404-840f-047d99e7ad8c` 的 `type:session` 记录中缺少 `deliveryContext` 字段，导致私聊回复被错误路由到大象 BridgeClient，出现 `staffId.notExisted: 400` 错误
3. **deliveryTo/defaultTarget 配置为空**：`openclaw.json` 中钉钉 channel 没有配置回复目标

### 修复步骤
1. 重启 gateway（`openclaw gateway run` 后台运行）
2. 在 `openclaw.json` 的 `channels.ddingtalk` 中新增：
   - `deliveryTo: "5155014812-513450819"`
   - `defaultTarget: "5155014812-513450819"`
3. 删除损坏的私聊 session 文件，让系统重建

### 靓仔钉钉信息
- userId: `5155014812-513450819`
- 昵称: 呦～写bug呐

### 教训
- 私聊不回复但群聊正常 → 优先检查私聊 session 的 deliveryContext 是否有 channel 字段
- `conversationId=single_XXXX` 出现在大象 BridgeClient 日志里 → session 路由配置错误
- deliveryTo 为空 → 主动发送会 400，但收消息不受影响

## 其他
- 靓仔提到笔记仓库 `09-前端面试题/工程化` 部分答案不够详细深入，已派 7 个子 Agent 并发完善（并发上限 5，先跑工程化/JS/React/性能优化/网络）
