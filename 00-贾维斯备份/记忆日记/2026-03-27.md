# 2026-03-27 日记

## 算法冲刺收官日 🎉

- 今天是 3 周算法冲刺（03-07 ~ 03-27）最后一天
- 已向钉钉 + 大象推送收官提醒（09:01）
- 靓仔尚未确认今日刷题完成状态

## 钉钉机器人断连（10:34 报告）

- **现象**：钉钉机器人无法响应靓仔消息
- **根因**：`dingtalk-stream` 通过 `http://127.0.0.1:8118` 代理连接钉钉服务器，日志中完全没有 "connect success" 记录，Stream WebSocket 握手失败
- **日志证据**：`/tmp/openclaw/openclaw-2026-03-27.log` 中只有 `[dingtalk-stream] Using proxy: http://127.0.0.1:8118`，无后续连接成功日志
- **历史参考**：与 03-10 那次故障一样（代理导致 WSS ECONNRESET），详见 `memory/ddingtalk-incidents.md`
- **建议修复**：
  1. `curl -x http://127.0.0.1:8118 https://api.dingtalk.com -I` 确认代理存活
  2. `openclaw gateway stop && openclaw gateway start` 重启 gateway
  3. 若无效，检查上游代理配置
- **状态**：⏳ 等待靓仔操作确认

## 大象 channel 正常

- 大象消息发送成功（09:01 收官提醒已送达）
- `daxiang:direct:single` session 75k/100k (75%)

## 其他

- OpenClaw 有可用更新（2026.3.24），靓仔未处理
- `openclaw status` 显示 DingTalk: enabled, configured, running（但实际 Stream 断连，状态显示不准确）

## Gateway Watchdog undefined 修复（12:10）

- **根因**：`gateway-watchdog` cron job 的 payload 字段名用了 `prompt` 而不是 `message`，导致 cron 系统读到 `undefined`
- **修复**：将 `jobs.json` 中 `gateway-watchdog` 的 `payload.prompt` 重命名为 `payload.message`
- **状态**：✅ 已修复，下次 5 分钟轮次将正常执行

## self-heal 检查（12:10）

- Gateway 进程正常运行（PID: 229007）
- WebSocket 断线 6 分钟后已自动重启（self-heal 已处理）
- 钉钉 + 大象 channel 均正常
- 无新的 cron 任务失败

## 算法冲刺今日提醒

- 今天（03-27）是冲刺收官日，算法记录中尚无今日刷题记录
- 已多次推送提醒（上午多次），靓仔尚未确认完成

## 心跳检查（13:31）

- Token 用量：23.1%，正常
- Self-heal：全部正常，钉钉 WebSocket 0.1分钟前刚重连成功
- Cron 任务失败：历史遗留失败（channel 配置问题、delivery failed），均已在 handledFailures 中记录
- 算法提醒：尝试向钉钉/大象发送收官日下午提醒，两个 channel 均发送失败（钉钉 staffId 问题，大象 conversationId=heartbeat 问题）
- 刷题记录：今日尚无记录
