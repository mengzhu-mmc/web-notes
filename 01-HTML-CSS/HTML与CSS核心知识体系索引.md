# HTML & CSS 核心知识体系索引

这是一份基于“机制驱动”与“场景方案”的 HTML/CSS 复习指南，告别碎片化字典记忆，聚焦面试核心与实战应用。

## 1. 核心渲染机制（面试高频）
理解 CSS 的底层工作原理，解决样式冲突、定位失效、性能卡顿等问题。

**推荐深度阅读：[👉 深入理解浏览器渲染机制与 CSS 底层模型](./01-底层渲染与机制/深入理解浏览器渲染机制与CSS底层模型.md) (含图解)**
- [x] **BFC（块级格式化上下文）**
  - 核心场景：清除浮动、解决 margin 塌陷、阻止元素被浮动元素覆盖。
- [x] **层叠上下文（Stacking Context）**
  - 核心场景：`z-index` 为什么不生效？元素的渲染层级关系。
- [x] **重绘与回流（Repaint & Reflow）**
  - 核心场景：性能优化、GPU 硬件加速（`transform`、`opacity`、`will-change`）。
- [x] **包含块（Containing Block）**
  - 核心场景：`absolute` 和 `fixed` 到底相对于谁定位？
- [x] **选择器优先级与样式层叠机制**

## 2. 现代布局系统
摒弃过时的 Float 布局，全面拥抱现代布局，掌握经典场景实现。
- [x] **Flexbox 弹性盒模型**
  - 核心应用：一维布局、水平垂直居中、自适应等分。
- [x] **Grid 网格布局**（可选进阶）
  - 核心应用：二维布局、复杂仪表盘、相册排版。
- [x] **经典场景方案库**
  - 水平垂直居中的 N 种方式
  - 多列等高布局
  - 圣杯/双飞翼布局（了解历史）
  - 粘性页脚（Sticky Footer）

## 3. 移动端与响应式适配
处理不同设备和屏幕尺寸的样式一致性问题。
- [x] **移动端适配方案**
  - `rem` / `vw` / `vh` 方案
  - 经典的 `1px` 边框变粗问题及解法
- [x] **响应式设计**
  - 媒体查询（Media Queries）
  - 新特性：容器查询（Container Queries）、`clamp()` 等函数

## 4. 前沿特性与工程化
掌握最新的 Web 平台能力以及如何高效管理 CSS 代码。
- [x] **CSS 新特性 (2023-2026)**
  - CSS 交互特性（`popover`, `dialog-closedBy`, 滚动驱动动画等）
  - 新单位（如 `lh`）
- [x] **CSS 工程化**
  - 预处理器（Sass/Less）、PostCSS
  - CSS Modules, CSS-in-JS, Tailwind 核心思想

## 5. HTML5 语义化与无障碍
- [x] **语义化标签与页面结构**
  - 对 SEO 和屏幕阅读器的意义
- [x] **DOM 解析与页面加载过程**
