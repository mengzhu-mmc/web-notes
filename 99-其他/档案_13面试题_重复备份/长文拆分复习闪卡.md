# 长文拆分复习闪卡

> 来源：已扩充的标准答案索引页。用于 Obsidian 快速复习，也可后续导出 Anki。

| 主题 | 问题 | 标准答案摘要 | 关键词 |
|---|---|---|---|
| 牛客网-面试题-网络 | HTTP 缓存怎么答？ | 先区分强缓存与协商缓存。强缓存通过 Cache-Control/Expires 直接复用本地资源；协商缓存通过 ETag/Last-Modified 向服务端确认资源是否变化... | Cache-Control、ETag、Last-Modified、hash 静态资源、HTML no-cache |
| 牛客网-面试题-网络 | HTTPS 为什么安全？ | HTTPS 在 HTTP 与 TCP 之间加入 TLS。它通过证书验证身份，通过密钥协商生成会话密钥，再用对称加密传输数据，同时用摘要和签名保证完整性，主要解决窃听、篡改和冒... | TLS、证书、密钥协商、对称加密、完整性 |
| 牛客网-面试题-网络 | 从输入 URL 到页面展示发生了什么？ | 可以按 DNS 解析、TCP/TLS 建连、HTTP 请求响应、浏览器解析 HTML、构建 DOM/CSSOM、生成渲染树、布局绘制、执行 JS、加载子资源这条链路回答，再补... | DNS、TCP、TLS、DOM、CSSOM、渲染流水线 |
| 牛客网-面试题-网络 | HTTP/1.1、HTTP/2、HTTP/3 的核心区别？ | HTTP/1.1 通过长连接和管线化改善连接复用，但仍有队头阻塞。HTTP/2 引入二进制分帧、多路复用、Header 压缩和 Server Push，解决应用层队头阻塞。H... | 长连接、多路复用、队头阻塞、QUIC、UDP |
| 牛客网-面试题-网络 | TCP 三次握手为什么不是两次？ | 三次握手的核心是让双方都确认自己的发送和接收能力，以及对方的发送和接收能力都正常。两次握手时服务端无法确认客户端是否收到自己的确认，也容易因为历史连接请求导致错误建立连接。 | SYN、ACK、历史连接、双向确认 |
| 牛客网-面试题-网络 | TCP 四次挥手为什么需要 TIME_WAIT？ | TIME_WAIT 主要有两个作用：保证最后一个 ACK 丢失时可以重传，确保对端能正常关闭；同时让旧连接中的延迟报文在网络中自然消失，避免影响相同四元组的新连接。 | FIN、ACK、TIME_WAIT、延迟报文 |
| 牛客网-面试题-网络 | DNS 解析过程怎么答？ | 浏览器会先查本地缓存、系统缓存、hosts 和本地 DNS；如果没有命中，本地 DNS 会从根域名服务器、顶级域名服务器、权威域名服务器逐级查询，最终得到 IP。工程上可通过... | DNS 缓存、递归查询、迭代查询、TTL、DNS Prefetch |
| 牛客网-面试题-网络 | CORS 跨域怎么答？ | CORS 是浏览器同源策略下的跨域资源访问机制。简单请求会直接发送并校验响应头；复杂请求会先发 OPTIONS 预检。服务端需要配置 Access-Control-Allow... | 同源策略、预检请求、OPTIONS、Credentials |
| 牛客网-面试题-网络 | Cookie、localStorage、sessionStorage 区别？ | Cookie 会随请求自动携带，适合会话标识但要注意体积和安全；localStorage 持久存在，适合非敏感本地配置；sessionStorage 只在当前标签页会话内有效... | Cookie、localStorage、sessionStorage、SameSite、HttpOnly |
| 牛客网-面试题-网络 | XSS 和 CSRF 怎么防？ | XSS 是注入恶意脚本，防护重点是输入校验、输出转义、CSP、避免直接 innerHTML、Cookie 设置 HttpOnly。CSRF 是借用户登录态发起伪造请求，防护重... | XSS、CSRF、CSP、SameSite、Token |
| 牛客网-面试题-网络 | CDN 为什么能加速？ | CDN 把静态资源缓存到离用户更近的边缘节点，减少网络传输距离和源站压力。它通常结合 DNS 调度、缓存策略、回源控制、压缩和 HTTP/2/3 使用。要关注缓存命中率、刷新... | CDN、边缘节点、缓存命中率、回源、预热 |
| 牛客网-面试题-网络 | 前端如何做网络性能优化？ | 可以从减少请求数、减少资源体积、提升缓存命中率、缩短关键链路、优化协议和提升并发利用率入手。典型手段包括资源压缩、代码分割、HTTP 缓存、CDN、preload/prefe... | 请求数、资源体积、缓存、preload、接口聚合 |
| 03-三、核心进阶 | 事件循环怎么答？ | JS 主线程执行同步代码，异步任务完成后进入任务队列。每轮宏任务执行完会清空当前微任务队列，然后再进入渲染和下一轮宏任务。Promise.then、queueMicrotas... | 宏任务、微任务、Promise、渲染时机 |
| 03-三、核心进阶 | 闭包是什么？ | 闭包是函数和其词法环境的组合。内部函数即使在外部函数执行结束后，仍然可以访问外部作用域变量。它常用于数据私有化、函数工厂、缓存，但也可能因为长期持有引用导致内存无法释放。 | 词法作用域、私有变量、缓存、内存泄漏 |
| 03-三、核心进阶 | 深拷贝怎么答？ | 浅拷贝只复制第一层引用，深拷贝会递归复制嵌套对象。工程上优先考虑 structuredClone 或成熟库；手写时要处理数组、对象、Date、RegExp、Map/Set、循... | structuredClone、循环引用、Map、Set、原型 |
| 03-三、核心进阶 | this 指向怎么判断？ | this 的指向取决于调用方式，而不是定义位置。默认调用指向全局或 undefined，隐式调用指向调用对象，显式调用由 call/apply/bind 指定，new 调用指... | 默认绑定、隐式绑定、显式绑定、new、箭头函数 |
| 03-三、核心进阶 | 原型链怎么答？ | 每个对象都有内部原型，构造函数有 prototype。访问属性时如果对象自身没有，会沿着原型链向上查找，直到 null。原型链用于属性共享和继承，但过深会影响查找，也容易出现... | prototype、__proto__、constructor、继承 |
| 03-三、核心进阶 | Promise 的状态和链式调用怎么理解？ | Promise 有 pending、fulfilled、rejected 三种状态，状态一旦 settled 就不可再变。then/catch/finally 会返回新的 P... | pending、fulfilled、rejected、thenable、链式调用 |
| 03-三、核心进阶 | async/await 和 Promise 的关系？ | async 函数本质返回 Promise，await 会暂停当前 async 函数后续执行，并等待右侧 Promise settled。await 后面的代码可以理解为放入微... | async、await、Promise、try/catch、微任务 |
| 03-三、核心进阶 | 防抖和节流区别？ | 防抖是在连续触发停止一段时间后只执行一次，适合搜索输入、窗口 resize 后处理。节流是在一段时间内最多执行一次，适合滚动监听、拖拽和高频上报。选择时看业务需要最后一次结果... | debounce、throttle、resize、scroll |
| 03-三、核心进阶 | 垃圾回收怎么答？ | JS 引擎会自动回收不可达对象。常见算法包括标记清除和分代回收。前端内存泄漏常来自全局引用、未清理定时器/事件监听、闭包长期持有 DOM、大对象缓存不释放等。 | 可达性、标记清除、分代回收、内存泄漏 |
| 03-三、核心进阶 | ES Module 和 CommonJS 区别？ | CommonJS 是运行时加载，导出的是值的拷贝或对象引用，主要用于 Node 历史生态。ES Module 是编译期静态结构，支持静态分析、Tree Shaking 和 l... | CJS、ESM、静态分析、Tree Shaking、live binding |
| 03-三、核心进阶 | Map 和 Object 怎么选？ | Object 更适合固定结构的数据模型，键主要是字符串或 Symbol。Map 适合动态增删键值、键类型不限、需要稳定迭代顺序和频繁读写的场景。WeakMap 适合用对象做弱... | Map、Object、WeakMap、键类型、迭代 |
| 03-三、核心进阶 | 如何判断数组、对象和类型？ | 基础类型可用 typeof，但 null 会返回 object。数组用 Array.isArray，对象可结合 Object.prototype.toString，实例关系可... | typeof、Array.isArray、instanceof、toString |
| 02-二、渲染性能 | 如何排查页面卡顿？ | 先用 Performance 录制定位长任务、频繁 layout、paint 或脚本执行热点；再结合 React Profiler 或业务埋点看具体组件和交互。优化时按减少计... | Performance、Long Task、Profiler、渲染面积 |
| 02-二、渲染性能 | 重排和重绘区别？ | 重排是元素几何信息变化导致浏览器重新计算布局，重绘是外观变化导致重新绘制但不一定影响布局。重排成本通常更高，应避免频繁读写 layout 属性，动画优先使用 transfor... | Layout、Paint、transform、opacity |
| 02-二、渲染性能 | 虚拟列表为什么有效？ | 虚拟列表只渲染可视区域及缓冲区内的节点，把大量 DOM 节点控制在稳定数量级，从而减少初始化、布局和更新成本。关键点是高度估算、滚动占位、缓冲区和动态高度处理。 | 虚拟列表、可视区、缓冲区、动态高度 |
| 02-二、渲染性能 | 浏览器渲染流水线是什么？ | 浏览器从 HTML/CSS 解析开始，构建 DOM 和 CSSOM，再生成渲染树，经过 Layout 计算几何信息，Paint 绘制像素，最后 Composite 合成图层。... | DOM、CSSOM、Layout、Paint、Composite |
| 02-二、渲染性能 | 如何减少强制同步布局？ | 强制同步布局通常来自先写 DOM 样式再立即读取 offsetWidth、getBoundingClientRect 等布局属性。优化方式是读写分离、批量更新、缓存布局结果，... | 强制同步布局、读写分离、rAF |
| 02-二、渲染性能 | CSS 动画为什么推荐 transform/opacity？ | transform 和 opacity 通常可以在合成线程处理，不必频繁触发布局和绘制，因此更适合高频动画。改变 width、height、top、left 等属性容易触发 ... | 合成层、GPU、transform、opacity |
| 02-二、渲染性能 | 长列表除了虚拟列表还能怎么优化？ | 可以分页、懒加载、骨架屏、服务端筛选排序、减少单项组件复杂度、稳定 key、缓存测量结果，以及对不可见区域暂停图片和复杂交互。虚拟列表适合数量很大且滚动浏览的场景。 | 分页、懒加载、稳定 key、测量缓存 |
| 02-二、渲染性能 | 如何优化图片渲染性能？ | 图片优化包括选择合适格式，使用 WebP/AVIF，按需裁剪尺寸，懒加载，响应式 srcset，设置 width/height 避免布局偏移，并对首屏关键图使用 preload。 | WebP、AVIF、lazy、srcset、CLS |
| 02-二、渲染性能 | requestAnimationFrame 有什么用？ | requestAnimationFrame 会在浏览器下一次绘制前执行，适合做动画和视觉更新。相比 setTimeout，它更贴合刷新节奏，页面隐藏时也可能暂停，能减少不必要... | rAF、动画、刷新率 |
| 02-二、渲染性能 | Web Worker 适合什么场景？ | Web Worker 适合把大计算、解析、压缩、加密、数据转换等任务移出主线程，避免阻塞交互。但 Worker 与主线程通信有序列化成本，不适合频繁传递大量复杂对象的小任务。 | Worker、主线程、序列化成本 |
| 02-二、渲染性能 | 如何优化首屏渲染体验？ | 首屏体验可通过减少关键资源体积、内联关键 CSS、延迟非关键 JS、预加载关键资源、SSR/SSG、骨架屏和图片尺寸占位优化。重点是让用户更早看到稳定且可交互的内容。 | 首屏、关键 CSS、SSR、骨架屏 |
| 02-二、渲染性能 | 如何衡量渲染性能优化效果？ | 可以结合实验室指标和真实用户指标。实验室用 Lighthouse、Performance；线上用 FCP、LCP、CLS、INP、长任务数量、JS 错误率和业务转化指标评估。 | FCP、LCP、CLS、INP、RUM |
| 01-一、Webpack | Loader 和 Plugin 区别？ | Loader 面向单个模块的内容转换，例如把 TS、Less、图片转成 Webpack 可处理的模块；Plugin 面向构建流程扩展，可以在编译生命周期中做资源注入、优化、分... | Loader、Plugin、模块转换、生命周期 |
| 01-一、Webpack | Webpack 构建慢怎么优化？ | 先用 speed-measure-webpack-plugin 或构建日志定位耗时。常见手段包括缩小 loader include 范围、开启 filesystem cach... | 构建缓存、include、esbuild、swc、resolve |
| 01-一、Webpack | Tree Shaking 生效条件？ | Tree Shaking 依赖 ES Module 的静态结构，生产模式下结合 sideEffects 标记和压缩器删除未使用代码。要避免 CommonJS、动态导出和有副作... | ESM、sideEffects、Terser、静态分析 |
| 01-一、Webpack | Webpack 的核心构建流程？ | Webpack 从 entry 出发，解析模块依赖，调用 loader 转换模块内容，构建依赖图，然后根据 chunk 规则分组，最后通过 plugin 扩展流程并输出 as... | entry、依赖图、chunk、assets |
| 01-一、Webpack | SourceMap 怎么选？ | 开发环境优先 eval-cheap-module-source-map，构建快且定位源码方便；生产环境可用 source-map 或 hidden-source-map 便于... | SourceMap、devtool、hidden-source-map |
| 01-一、Webpack | Code Splitting 怎么做？ | Code Splitting 可以通过入口拆分、动态 import 和 splitChunks 做。业务路由适合动态 import 懒加载，公共依赖适合 splitChunk... | import()、splitChunks、路由懒加载、缓存 |
| 01-一、Webpack | hash、chunkhash、contenthash 区别？ | hash 和整个构建相关，任意文件变化都可能改变；chunkhash 和 chunk 内容相关；contenthash 和具体文件内容相关。生产环境静态资源通常使用 cont... | hash、chunkhash、contenthash、缓存 |
| 01-一、Webpack | Babel 在 Webpack 中的作用？ | Babel 主要负责语法转换和按需注入 polyfill。Webpack 负责模块打包，babel-loader 把模块交给 Babel 转换。工程上要结合 browsers... | Babel、preset-env、core-js、browserslist |
| 01-一、Webpack | Webpack 5 Module Federation 是什么？ | Module Federation 允许多个独立构建的应用在运行时共享模块，常用于微前端。它可以让 host 动态加载 remote 暴露的组件或模块，同时共享 React ... | Module Federation、微前端、host、remote |
| 01-一、Webpack | 如何分析打包体积？ | 可以使用 webpack-bundle-analyzer、stats.json 或构建平台分析各 chunk、依赖和重复包。优化方向包括按需引入、Tree Shaking、拆... | bundle analyzer、stats、按需引入 |
| 01-一、Webpack | externals 适合什么场景？ | externals 用于把某些依赖排除在打包之外，运行时从 CDN 或外部环境获取。它能减少包体积，但会增加外部资源稳定性、版本一致性和加载顺序管理成本。 | externals、CDN、包体积、版本 |
| 01-一、Webpack | 如何设计多环境构建配置？ | 通常拆成 common、dev、prod 配置。开发环境关注热更新、SourceMap 和构建速度；生产环境关注压缩、缓存、拆包、资源路径和安全。环境变量要通过统一配置注入，... | 多环境、dev、prod、环境变量 |
| React性能优化指南 | React 中 memo 什么时候有用？ | memo 适合 props 稳定且组件渲染成本较高的场景。它不是默认优化手段，如果 props 每次都是新对象或新函数，或者组件本身很轻，memo 反而可能增加比较成本。 | memo、props 稳定、浅比较 |
| React性能优化指南 | useMemo 和 useCallback 怎么选？ | useMemo 缓存计算结果，useCallback 缓存函数引用。它们主要用于避免昂贵计算重复执行，或配合 memo 稳定子组件 props。不要为了“看起来优化”滥用。 | useMemo、useCallback、引用稳定 |
| React性能优化指南 | startTransition 解决什么问题？ | startTransition 用来标记非紧急更新，让 React 优先响应输入、点击等紧急交互，再处理列表过滤、搜索结果渲染等可延后的更新，从而改善交互流畅度。 | React 18、transition、并发渲染 |
| React性能优化指南 | React 如何定位性能问题？ | 先用 React DevTools Profiler 看哪些组件频繁渲染、渲染耗时和提交次数，再结合 Performance 看主线程长任务。定位后判断是状态设计、props... | Profiler、commit、render、Performance |
| React性能优化指南 | 为什么不建议无脑使用 useCallback？ | useCallback 本身也有依赖比较和闭包维护成本。如果函数没有传给 memo 子组件，或不是依赖稳定性的 Hook 参数，缓存它通常没有收益。优化要基于测量，而不是机械添加。 | useCallback、闭包、依赖数组、优化成本 |
| React性能优化指南 | React 列表性能怎么优化？ | 列表优化包括稳定 key、减少 item 组件渲染成本、分页或虚拟列表、懒加载图片、避免在 render 中创建大量新对象，以及把列表项状态局部化。数据量很大时优先虚拟列表。 | key、虚拟列表、局部状态 |
| React性能优化指南 | Context 为什么可能导致性能问题？ | Context value 变化会让消费该 Context 的组件重新渲染。如果把频繁变化的大对象放进 Context，影响范围会很大。优化方式是拆分 Context、稳定 ... | Context、value、选择性订阅 |
| React性能优化指南 | 状态应该放在哪里？ | 状态应放在真正需要它的最小公共父级，避免过度提升导致大范围重渲染。局部交互状态优先放组件内部，跨页面或跨模块共享状态再考虑全局状态管理。 | 状态提升、局部状态、全局状态 |
| React性能优化指南 | useDeferredValue 和 startTransition 区别？ | startTransition 是把某次状态更新标记为非紧急；useDeferredValue 是让某个值的更新延后，常用于输入值和搜索结果渲染解耦。前者控制更新，后者控制值... | useDeferredValue、startTransition、非紧急更新 |
| React性能优化指南 | React 中 key 为什么重要？ | key 帮助 React 在 diff 时识别节点身份。稳定 key 可以复用组件实例和状态；使用 index 作为 key 在插入、删除、排序时可能导致状态错乱和不必要渲染。 | key、diff、状态复用 |
| React性能优化指南 | React 组件拆分如何影响性能？ | 合理拆分可以缩小状态变化影响范围，让不相关组件避免重渲染。但过度拆分会增加组件层级和理解成本。性能优化中更重要的是状态边界、memo 边界和数据流稳定。 | 组件拆分、状态边界、memo 边界 |
| React性能优化指南 | React 性能优化如何和业务指标关联？ | 不要只看组件渲染次数，要关联首屏、交互延迟、输入响应、列表滚动 FPS、接口耗时和业务转化。优化前后用 Profiler、RUM 和埋点证明收益。 | 业务指标、RUM、INP、FPS |
| 03-三、高级优化专题 | 前端性能优化如何系统回答？ | 先明确目标指标，例如 LCP、INP、CLS、首屏耗时和资源体积；再从加载链路、渲染链路、运行时交互、缓存策略和监控闭环拆解。最后结合项目讲优化前后的指标变化，避免只罗列手段。 | 指标体系、加载、渲染、缓存、监控 |
| 03-三、高级优化专题 | 首屏慢怎么排查？ | 先用 Performance、Lighthouse、WebPageTest 或 RUM 数据确认瓶颈在网络、资源、接口、渲染还是 hydration。常见优化包括压缩资源、拆... | 首屏、LCP、TTFB、hydration、SSR |
| 03-三、高级优化专题 | 长任务怎么治理？ | 长任务会阻塞主线程，导致输入响应变慢。治理方式包括拆分大计算、时间切片、requestIdleCallback、Web Worker、虚拟列表、减少同步布局，以及在 Reac... | Long Task、INP、Worker、时间切片 |
| 03-三、高级优化专题 | 如何建立前端性能指标体系？ | 指标体系应包含加载体验、交互体验、视觉稳定性、资源成本和业务结果。常用指标有 FCP、LCP、CLS、INP、TTFB、长任务、资源体积、缓存命中率和转化率。线上以 RUM ... | FCP、LCP、CLS、INP、RUM |
| 03-三、高级优化专题 | LCP 优化有哪些手段？ | 先找出 LCP 元素，通常是首屏大图、标题或主要内容块。优化方式包括降低 TTFB、预加载 LCP 资源、压缩图片、使用合适尺寸和格式、避免阻塞 CSS/JS、SSR 输出关... | LCP、preload、图片、TTFB、关键资源 |
| 03-三、高级优化专题 | INP 优化有哪些手段？ | INP 关注用户交互到下一次绘制的延迟。优化方向是减少主线程长任务、降低事件处理函数复杂度、拆分渲染更新、避免同步布局、使用 transition/Worker，并优先治理高... | INP、交互延迟、事件处理、主线程 |
| 03-三、高级优化专题 | CLS 优化有哪些手段？ | CLS 来自非预期布局偏移。优化方式包括为图片和广告预留尺寸、避免首屏动态插入内容、字体加载使用合适策略、动画使用 transform，骨架屏尺寸要与真实内容接近。 | CLS、尺寸占位、字体、骨架屏 |
| 03-三、高级优化专题 | 性能监控 SDK 应该采集什么？ | 基础采集包括 Web Vitals、资源加载、接口耗时、JS 错误、白屏、长任务、页面路由和用户环境。还要支持采样、上报合并、异常去重、用户会话关联和隐私字段脱敏。 | SDK、Web Vitals、错误、采样、脱敏 |
| 03-三、高级优化专题 | 如何做性能优化闭环？ | 闭环包括发现问题、定位瓶颈、制定方案、灰度上线、指标验证和持续监控。每次优化都要有基线、目标、实验范围和回滚策略，避免只做一次性手工优化。 | 基线、灰度、验证、回滚、持续监控 |
| 03-三、高级优化专题 | 资源加载优先级怎么优化？ | 首屏关键资源应尽早加载，例如 preload 关键 CSS、字体和 LCP 图片；非关键资源延迟或懒加载；prefetch 用于可能访问的后续路由。要避免过度 preload... | preload、prefetch、懒加载、优先级 |
| 03-三、高级优化专题 | Hydration 慢怎么优化？ | Hydration 慢通常来自 JS 包过大、组件树复杂或首屏交互绑定成本高。可以通过减少客户端 JS、组件级懒加载、Streaming SSR、选择性 hydration、... | hydration、Streaming SSR、RSC、islands |
| 03-三、高级优化专题 | 性能优化如何避免负收益？ | 任何优化都要评估复杂度、维护成本、兼容性和真实收益。例如过度拆包可能增加请求和调度成本，过度缓存可能带来更新问题，过度 memo 可能增加比较成本。 | 负收益、复杂度、维护成本、取舍 |
| 牛客网-面试题-场景设计 | 如何设计一个前端权限系统？ | 先区分路由权限、菜单权限、按钮权限和数据权限。前端通过登录态、用户角色/权限点、路由守卫和组件级权限控制展示，后端必须做最终鉴权。工程上要考虑权限缓存、动态路由、刷新恢复、灰... | 路由权限、按钮权限、数据权限、后端鉴权 |
| 牛客网-面试题-场景设计 | 如何设计大文件上传？ | 核心是分片、并发上传、断点续传、秒传和失败重试。前端计算文件 hash，按固定大小切片并发上传，服务端记录已上传分片，全部完成后合并。还要考虑暂停恢复、进度展示、限流、校验和... | 分片、hash、断点续传、秒传、并发 |
| 牛客网-面试题-场景设计 | 如何设计一个通用表单系统？ | 可以从 Schema 驱动、字段组件映射、校验规则、联动逻辑、布局、权限和提交适配展开。重点是把业务差异配置化，把复杂联动和异步校验封装为可维护的扩展点。 | Schema、校验、联动、字段组件 |
| 牛客网-面试题-场景设计 | 如何设计一个通用弹窗系统？ | 通用弹窗要解决调用方式、层级管理、生命周期、数据传递、关闭确认和无障碍。可以提供命令式 API 和声明式组件两种方式，内部维护弹窗队列、z-index、滚动锁定和统一样式。 | 弹窗、命令式 API、队列、z-index |
| 牛客网-面试题-场景设计 | 如何设计前端埋点系统？ | 埋点系统要支持曝光、点击、停留、转化和错误等事件。设计上包括事件规范、自动采集、手动埋点、队列缓存、批量上报、失败重试、采样和隐私脱敏。关键是保证数据准确和业务可分析。 | 埋点、曝光、批量上报、采样、脱敏 |
| 牛客网-面试题-场景设计 | 如何设计低代码页面搭建？ | 低代码核心是物料体系、Schema 协议、画布编辑、属性面板、渲染引擎和发布链路。难点在组件扩展、版本兼容、表达式/联动、安全沙箱、权限和回滚。 | 低代码、物料、Schema、渲染引擎 |
| 牛客网-面试题-场景设计 | 如何设计一个复杂表格？ | 复杂表格要考虑列配置、排序筛选、分页、固定列、虚拟滚动、批量操作、权限、单元格编辑和导出。大数据量场景下需要服务端分页/筛选，并控制单元格渲染成本。 | 表格、虚拟滚动、服务端分页、列配置 |
| 牛客网-面试题-场景设计 | 如何设计前端错误监控？ | 错误监控包括 JS 运行时错误、Promise 未捕获错误、资源加载失败、接口错误和白屏。SDK 要采集上下文、路由、用户环境、 sourcemap 还原、去重聚合，并提供告... | 错误监控、sourcemap、白屏、告警 |
| 牛客网-面试题-场景设计 | 如何设计搜索联想？ | 搜索联想要处理输入防抖、请求取消、缓存、空态、加载态、错误态和键盘操作。接口返回要防止乱序覆盖，可以用 AbortController 或请求序号保证只展示最新结果。 | 搜索、防抖、AbortController、缓存 |
| 牛客网-面试题-场景设计 | 如何设计主题切换？ | 主题切换可以基于 CSS Variables 维护设计 token，运行时切换根节点 class 或变量。要考虑首屏防闪、系统主题、用户偏好持久化、图片/图表主题和第三方组件适配。 | 主题、CSS Variables、Token、防闪 |
| 牛客网-面试题-场景设计 | 如何设计前端国际化？ | 国际化包括文案资源、语言检测、运行时切换、日期货币格式、复数规则、RTL、懒加载语言包和缺失文案兜底。工程上要提供提取、校验和翻译协作流程。 | i18n、语言包、Intl、RTL、兜底 |
| 牛客网-面试题-场景设计 | 如何设计离线缓存能力？ | 离线能力通常基于 Service Worker、Cache Storage 和本地存储。静态资源可预缓存，接口数据可按策略缓存。要处理版本更新、缓存淘汰、离线提示、数据冲突和... | Service Worker、Cache Storage、离线、版本更新 |
| TypeScript基础到进阶 | type 和 interface 怎么选？ | interface 更适合描述对象结构和可扩展声明合并，type 更适合联合类型、交叉类型、条件类型等组合表达。团队中通常对象模型优先 interface，复杂类型运算优先 ... | interface、type、声明合并、联合类型 |
| TypeScript基础到进阶 | unknown 和 any 区别？ | any 会绕过类型检查，使用后基本失去类型安全；unknown 表示未知类型，使用前必须做类型收窄。因此对外部输入、JSON 解析、错误对象等不确定数据，优先用 unknow... | unknown、any、类型收窄、类型安全 |
| TypeScript基础到进阶 | 泛型解决什么问题？ | 泛型把类型作为参数传入，使函数、类或组件在保持类型安全的同时支持多种数据结构。它常用于请求封装、组件 props、工具函数和类型工具。 | 泛型、类型参数、复用、约束 |
| TypeScript基础到进阶 | 联合类型和交叉类型区别？ | 联合类型表示值可以是多个类型之一，使用时只能访问公共成员，需要类型收窄。交叉类型表示同时满足多个类型，常用于组合对象能力。可以简单理解为 union 是“或”，interse... | union、intersection、类型收窄 |
| TypeScript基础到进阶 | 类型收窄有哪些方式？ | 常见方式包括 typeof、instanceof、in、字面量判等、判别联合、自定义类型守卫和控制流分析。类型收窄的目标是在安全分支内获得更具体的类型。 | typeof、in、instanceof、判别联合、类型守卫 |
| TypeScript基础到进阶 | never 类型有什么用？ | never 表示永远不会出现的值，常用于抛错函数、无限循环和穷尽检查。在判别联合的 switch 中，可以用 never 确保所有分支都被处理，遗漏类型时编译器会报错。 | never、穷尽检查、判别联合 |
| TypeScript基础到进阶 | 条件类型怎么理解？ | 条件类型形如 T extends U ? X : Y，根据类型关系选择不同结果。它常用于工具类型和类型推导。配合泛型和 infer，可以从函数、数组、Promise 等结构中... | conditional type、extends、infer |
| TypeScript基础到进阶 | infer 有什么用？ | infer 用于在条件类型中声明待推断的类型变量。例如从 Promise<T> 中提取 T，从函数类型中提取参数或返回值。它让复杂类型转换更可读。 | infer、ReturnType、Parameters、Awaited |
| TypeScript基础到进阶 | keyof 和 typeof 在类型中怎么用？ | keyof 获取对象类型的键联合，typeof 在类型上下文中获取变量的静态类型。二者常配合 as const 使用，把运行时对象转换为可复用的类型约束。 | keyof、typeof、as const、键联合 |
| TypeScript基础到进阶 | Partial、Pick、Omit 怎么实现？ | 这些工具类型基于映射类型和 keyof。Partial 把属性变可选，Pick 选取指定键，Omit 通常基于 Pick 和 Exclude 排除指定键。理解它们有助于手写业... | Partial、Pick、Omit、mapped type |
| TypeScript基础到进阶 | React 中如何给组件 props 建模？ | 简单组件可直接定义 Props interface；带 children 用 PropsWithChildren 或显式 children；事件处理使用 React.Mous... | React Props、children、事件类型、泛型组件 |
| TypeScript基础到进阶 | 如何给接口请求做类型封装？ | 可以定义统一的 Response<T>、PageResult<T> 和 request<T>，让调用方传入响应数据类型。外部数据仍需运行时校验，TypeScript 只能保证... | request<T>、Response<T>、运行时校验 |
| week1-answers | 双指针题怎么识别？ | 当题目涉及有序数组、区间收缩、原地修改、两端逼近或快慢遍历时，优先考虑双指针。回答时说明指针含义、移动条件、循环不变量和复杂度。 | 双指针、有序数组、快慢指针、循环不变量 |
| week1-answers | 滑动窗口题怎么识别？ | 当题目要求连续子数组或子串的最长、最短、计数，并且窗口状态可增量维护时，可以考虑滑动窗口。关键是明确右指针扩张、左指针收缩条件和窗口内维护的数据。 | 滑动窗口、连续子串、窗口状态 |
| week1-answers | 算法题复盘记录什么？ | 不要只记录代码，要记录题型标签、为什么想到这个方法、关键不变量、复杂度、错因和相似题。这样下次才能快速迁移。 | 复盘、题型标签、错因、相似题 |
| week1-answers | 哈希表题怎么识别？ | 当题目需要快速判断元素是否出现、统计频次、查找配对关系或维护映射时，优先考虑哈希表。典型题包括两数之和、字母异位词、最长连续序列和前缀和计数。 | 哈希表、频次、映射、前缀和 |
| week1-answers | 前缀和适合什么题？ | 前缀和适合快速计算区间和，尤其是多次区间查询或子数组和相关问题。结合哈希表可以把“某段和等于 k”转化为查找历史前缀和。 | 前缀和、区间和、子数组和 |
| week1-answers | 栈题怎么识别？ | 当题目涉及最近未匹配元素、括号匹配、单调关系、表达式求值或撤销操作时，可以考虑栈。单调栈常用于下一个更大元素、每日温度和柱状图面积。 | 栈、括号、单调栈、最近关系 |
| week1-answers | 队列和 BFS 有什么关系？ | BFS 通常用队列维护当前层待访问节点，先进先出保证按层扩展。适合最短步数、层序遍历和状态扩散问题。需要记录 visited 防止重复访问。 | 队列、BFS、层序、visited |
| week1-answers | 二分查找怎么避免写错？ | 先明确搜索区间是闭区间还是左闭右开，再统一循环条件、mid 计算和边界更新。lower_bound 类问题重点是保持答案区间不丢失，最后验证返回位置是否有效。 | 二分、边界、lower_bound |
| week1-answers | 链表题常用技巧？ | 链表题常用 dummy 节点简化头节点处理，快慢指针判断环或找中点，反转链表要保存 next，合并链表要维护尾指针。关键是画指针变化，避免断链。 | 链表、dummy、快慢指针、反转 |
| week1-answers | 复杂度怎么表达？ | 时间复杂度说明主要循环或递归规模，空间复杂度说明额外数据结构和递归栈。面试中要主动比较暴力解和优化解复杂度变化，体现优化价值。 | 时间复杂度、空间复杂度、递归栈 |
| week1-answers | 原地修改数组要注意什么？ | 原地修改通常用读写指针。读指针遍历原数组，写指针维护下一个可写位置。要明确是否允许改变元素顺序，以及返回新长度还是新数组。 | 原地修改、读写指针、新长度 |
| week1-answers | 字符串题常见突破口？ | 字符串题常见方法包括哈希计数、双指针、滑动窗口、栈、前缀函数或动态规划。先判断是否要求连续、是否涉及匹配、是否需要保持顺序。 | 字符串、哈希、窗口、匹配 |
| week3-answers | DP 题怎么回答？ | 先定义 dp 数组含义，再说明状态转移方程、初始化、遍历顺序和复杂度。如果能空间优化，再说明优化前后依赖关系是否允许压缩。 | DP、状态定义、转移方程、初始化 |
| week3-answers | 回溯模板是什么？ | 回溯可以按路径、选择列表、结束条件组织。每层做选择、递归、撤销选择；剪枝用于提前排除不可能分支。排列、组合、子集、棋盘类问题都可套这个框架。 | 回溯、路径、选择、撤销、剪枝 |
| week3-answers | 树的递归题怎么稳住？ | 先明确当前函数处理以 root 为根的子树要返回什么，再决定在前序、中序还是后序位置处理逻辑。不要在脑中展开所有递归，重点维护函数语义。 | 树、递归定义、返回值、遍历位置 |
| week3-answers | 01 背包怎么答？ | 01 背包每个物品只能选一次。状态通常定义为前 i 个物品在容量 j 下的最大价值，转移为不选或选当前物品。压缩到一维时容量必须倒序遍历，避免同一物品被重复使用。 | 01 背包、倒序遍历、状态压缩 |
| week3-answers | 完全背包和 01 背包区别？ | 完全背包每个物品可以选无限次。一维压缩时容量通常正序遍历，这样当前物品可以在本轮被重复使用。01 背包倒序，完全背包正序，是高频易错点。 | 完全背包、正序遍历、重复选择 |
| week3-answers | 最长递增子序列怎么答？ | 基础 DP 定义 dp[i] 为以 i 结尾的 LIS 长度，枚举 j<i 转移，复杂度 O(n²)。优化版用贪心加二分维护每个长度的最小结尾，复杂度 O(n log n)。 | LIS、DP、贪心、二分 |
| week3-answers | 图遍历 DFS 和 BFS 怎么选？ | DFS 适合连通性、路径搜索、拓扑递归和回溯类问题；BFS 适合无权图最短路径和按层扩散。两者都要维护 visited，并根据题意选择邻接表或矩阵建图。 | DFS、BFS、visited、邻接表 |
| week3-answers | 拓扑排序解决什么问题？ | 拓扑排序用于有向无环图中的依赖顺序问题，例如课程表、任务编排。常用入度队列：入度为 0 的点入队，出队后删除其边，最后判断是否处理完所有节点。 | 拓扑排序、DAG、入度、依赖 |
| week3-answers | 贪心题怎么证明？ | 贪心要说明局部最优为什么能推出全局最优。常见证明方式包括交换论证、排序后选择、反证法。不能只说“每次选当前最优”，要解释为什么不会影响后续最优性。 | 贪心、交换论证、局部最优 |
| week3-answers | 区间问题常见套路？ | 区间问题通常先排序。合并区间按左端点排序并维护当前右边界；无重叠区间可按右端点排序做贪心；会议室问题可用堆或扫描线处理并发数量。 | 区间、排序、贪心、扫描线 |
| week3-answers | 并查集适合什么题？ | 并查集适合动态连通性、分组、朋友圈、岛屿合并和 Kruskal。核心操作是 find 和 union，路径压缩与按秩合并可以把复杂度降到近似常数。 | 并查集、连通性、路径压缩、union |
| week3-answers | 面试中算法卡住怎么办？ | 先说暴力解保证有思路，再分析瓶颈并尝试用哈希、排序、双指针、DP 或图搜索优化。卡住时主动讲状态定义、边界和复杂度，让面试官能看到推理过程。 | 暴力解、瓶颈、优化、推理过程 |
